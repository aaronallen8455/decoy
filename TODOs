Write docs
Assign an ID to rules so that they can be removed based on that ID
 - withRule function
Allow for deleting rules via the _rules endpoint
Path component that matches anything and discards it
File response should be a mustache template
